import { ExternalLink } from '.'

export const UsernameSettings = ExternalLink('/settings/username')

export const PasswordSettings = ExternalLink('/settings/password')

export const EmailSettings = ExternalLink('/settings/email')

export const Logout = ExternalLink('/logout')
